
110603 TEST LEARNING
GL S1)	
	unique list of [^U]Candidates OK!
	init with leafs OK!
	use same bottom-up features OK!
	pass hg edges mask OK!
	pop best scoring leaf OK!
	check if leaf is has correct edge OK!
	if not (loop until the first false) update weigths with PA OK!
	rescore queue
	new top must be correct

110603
	idea for paper title "Linear-Time Undirected [Parsing, Decoding] Algorithm (for Macine Translation)"
